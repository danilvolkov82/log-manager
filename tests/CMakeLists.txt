# Keep test folder wiring consistent with sibling projects.
# Add test executables and gtest_discover_tests(...) here.
message(STATUS "BUILD_TESTS is ON, but no log-manager tests are configured yet.")
